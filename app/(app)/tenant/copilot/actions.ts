export async function getCopilotSettings() {
  // Placeholder: return basic settings
  return { enabled: false }
}

export async function updateCopilotSettings(settings: any) {
  // No-op placeholder
  return { success: true }
}
